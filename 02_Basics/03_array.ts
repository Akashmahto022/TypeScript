const superHeros: object[] = []

type User = {
    name: string;
    isActive: boolean
}

const allUser: User[] = []

allUser.push({name: "akash", isActive: true})

const MLModels: number[][] = [
    [555, 2525, 4122],
    []
]
 
superHeros.push({ name: "Spiderman", power: 123 })


export {}